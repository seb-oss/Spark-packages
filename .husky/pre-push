#!/bin/sh
# Only run smoketest when pushing to main
while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Pushing to main — running smoketest..."
    yarn smoketest
  fi
done
